//! Layout constants shared across page and widget components.

/// Maximum width (in pixels) for `Adw.Clamp` content areas.
///
/// Keeps page content aligned at a consistent width across the app (details
/// pages, search, and other clamped views).
pub const CLAMP_MAX_SIZE: i32 = 1600;
